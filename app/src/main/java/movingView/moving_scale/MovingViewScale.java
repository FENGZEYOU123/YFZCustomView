package movingView.moving_scale;

import android.content.Context;
import android.util.AttributeSet;
import android.widget.LinearLayout;

import androidx.annotation.Nullable;

public class MovingViewScale extends LinearLayout {
    public MovingViewScale(Context context) {
        super(context);
    }
    public MovingViewScale(Context context, @Nullable AttributeSet attrs) {
        super(context, attrs);
    }
}
